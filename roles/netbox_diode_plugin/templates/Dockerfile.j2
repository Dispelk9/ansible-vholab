# Auto-generated by ansible-vholab (netbox_diode_plugin role). Do not edit by
# hand on the target host - edit the Jinja2 template in the Ansible repo
# instead and re-run the playbook.
#
# Extends the exact upstream image already running for this NetBox install
# (detected live in tasks/prep.yml, since roles/netbox tracks netbox-docker's
# floating "release" branch rather than a pinned tag) and layers the Diode
# NetBox plugin on top of it. The upstream netbox-docker project/image is
# never modified.
FROM {{ netbox_upstream_image }}

COPY plugin_requirements.txt /opt/netbox/plugin_requirements.txt
RUN {{ netbox_diode_pip_install_cmd }}
